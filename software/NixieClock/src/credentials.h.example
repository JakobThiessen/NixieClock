#pragma once

// Copy this file to credentials.h and fill in your WiFi credentials.
// credentials.h is excluded from git (see .gitignore).

#define AP_SSID "YOUR_SSID"
#define AP_PASS "YOUR_PASSWORD"
